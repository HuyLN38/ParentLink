import 'package:flutter/material.dart';

class SentMailPage extends StatelessWidget {
  const SentMailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}